spring:
  datasource:
    url: jdbc:h2:mem:lab2
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
      path: /h2-console
    datasource:
      url: jdbc:h2:mem:lab2
      driverClassName: org.h2.Driver
      username: sa
      password:


server:
  port: 8085